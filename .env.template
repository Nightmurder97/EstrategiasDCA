# CORE API KEYS (Secreto - DEBEN estar en .env)
CMC_API_KEY=your_coinmarketcap_api_key_uuid_format
BINANCE_API_KEY=your_binance_api_key_64chars
BINANCE_API_SECRET=your_binance_api_secret_64chars
ETHERSCAN_API_KEY=your_etherscan_api_key
BSCSCAN_API_KEY=your_bscscan_api_key
SOLSCAN_API_KEY=your_solscan_api_key # Formato JWT
DEEPSEEK_API_KEY=your_deepseek_api_key
# COINGECKO_API_KEY=your_coingecko_pro_api_key # Descomentar si se usa la API Pro de CoinGecko

# EMAIL CONFIGURATION (Secreto - DEBEN estar en .env si se usan notificaciones por email)
DCA_EMAIL=your_sender_email_address
DCA_EMAIL_PASSWORD=your_sender_email_password
DCA_NOTIFICATION_EMAIL=your_recipient_email_address

# DATABASE URL (Secreto si contiene credenciales, o si la ubicación es sensible)
DATABASE_URL=sqlite:///data/dca_trading.db # Ejemplo para SQLite local

# TRADING PARAMETERS (Ejemplos, pueden ser sobrescritos por configuración en código o YAML si se implementa)
# WEEKLY_INVESTMENT=100.0
# LOOKBACK_PERIOD=90
# PORTFOLIO_WEIGHTS_JSON='{"BTCUSDT":0.5,"ETHUSDT":0.3,"ADAUSDT":0.2}' # Ejemplo si se cargan pesos desde env

# NOTA: Los Base URLs y Endpoints de las APIs generalmente no son secretos y pueden
# estar definidos en el código de configuración (config_validator.py) a menos que
# necesiten ser altamente configurables por entorno.
